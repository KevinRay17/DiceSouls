using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Fungus;

public class FungusTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
     //   Flowchart.ExecuteBlock("Event2");   
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
